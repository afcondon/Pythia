// FFI stub for PureScript compiler - actual implementation is in UMAP.py
export const fitTransformImpl = (vectors, nNeighbors, minDist, nComponents, metric) => [];
export const projectWithLabelsImpl = (data, nNeighbors, minDist, metric) => [];
export const getDemoEmbeddings = [];
